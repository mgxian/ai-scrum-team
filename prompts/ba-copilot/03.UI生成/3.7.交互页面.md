### **复杂交互页面生成器****任务：**根据

业务需求如下：

【$ optimized_requirements

】

数据模型如下：

【$data_model】

UI规范如下：

【$specification】

**核心原则：**1. **从概念到代码**：请在生成代码前，先进行简明的设计思考，阐述你对布局、状态和交互路径的理解。

2. **渐进增强**：代码应遵循从静态布局到动态交互的实现层次。

3. **模块化与清晰度**：虽然输出为单个HTML文件，但代码结构（CSS, JavaScript）应模块化、注释清晰，体现关注点分离。

---

### **你的输出结构**#### **第一部分：设计决策与思路*** **页面核心目标**：用一句话总结本页面要解决的主要用户任务。

* **布局解构**：基于需求，描述你将如何划分页面区域（例如：顶部导航区、左侧资源面板、中央画布/工作区、右侧属性面板、底部状态栏）。说明每个区域的核心功能。

* **状态管理设计**：定义本应用最关键的1-2个全局状态（例如：`currentSelectedItem`, `workflowData`），并简要说明它们如何在组件间流动。

* **非线性路径设计**：具体描述你将为完成主要任务设计的至少2种不同操作路径。

* **关键技术选型说明**：简述你将如何在纯原生HTML/JS中模拟实现状态管理、拖拽、实时更新等复杂特性（例如：使用自定义事件、特定JS API）。

#### **第二部分：完整HTML页面代码**生成一个完整的`<!DOCTYPE html>`文件。代码内部应结构良好，并包含以下部分：

1. **HTML骨架**：

*包含必要的`<meta>`标签，确保响应式视图。

*主体结构使用语义化或带有明确CSS类名的`<div>`，对应你在**第一部分**描述的布局。

2. **CSS样式（位于`<style>`标签内）**：

* **基础与变量**：定义`:root`颜色变量、字体、基础重置。

* **布局CSS**：使用Flexbox/Grid实现之前描述的页面骨架，确保核心区域功能明确。

* **组件CSS**：为按钮、列表、卡片、输入框等UI元素设计样式，符合UI规范。

* **状态CSS**：定义交互状态类（如`.selected`, `.dragging`, `.hidden`）。

* **响应式考虑**：至少包含一个简单的媒体查询，调整布局以适应较小屏幕。

3. **JavaScript逻辑（位于`<script>`标签内）**：

* **模块化组织**：代码应按功能分区，使用清晰的注释分隔。

* **状态管理区**：定义核心应用状态（对象），并创建用于状态更新和通知的简易机制（例如：一个发布订阅模式的简易实现，或一个触发自定义事件的`updateState`函数）。

* **组件初始化区**：在DOM加载后，获取关键DOM元素并设置初始状态。

* **事件监听与交互区**：

*实现基础事件（点击、输入）。

* **实现至少一个核心复杂交互**（如**拖拽排序**或**多步骤表单联动**），提供详细注释。

*实现一种非线性操作路径的交互。

*添加快捷键支持示例（例如，按`Delete`键删除选中项）。

* **工具函数区**：包含防抖、节流、数据格式转换等辅助函数。

* **模拟数据与实时更新**：使用`setInterval`或`setTimeout`模拟数据变化，并更新UI，以体现实时性。提供清晰的模拟逻辑说明。

---

### **示例结构参考（增强版）**```html <!DOCTYPE html> <html lang="zh-CN"> <head> <meta charset="UTF-8"> <meta name="viewport" content="width=device-width, initial-scale=1.0"> <title>$根据需求生成的页面标题</title> <style> /* === CSS变量与基础重置=== */ :root --primary-color: #007bff; --bg-color: #f5f5f5; /* ... */
* box-sizing: border-box; margin: 0; padding: 0;

/* ===主布局（使用Grid/Flexbox）=== */ .app-container display: grid; grid-template-areas: ... ; height: 100vh; .top-nav grid-area: nav; .left-panel grid-area: left; .main-workspace grid-area: main; position: relative; /*为拖拽画布准备*/ .right-panel grid-area: right; .status-bar grid-area: status;

/* ===组件样式（卡片、按钮、列表）=== */ .draggable-card cursor: grab; transition: transform 0.2s; .draggable-card.dragging opacity: 0.5; cursor: grabbing;

/* ===响应式设计=== */ @media (max-width: 768px) .app-container grid-template-columns: 1fr; </style> </head> <body> <div class="app-container"> <header class="top-nav">...</header> <aside class="left-panel"> <ul class="resource-list" id="resourceList"></ul> </aside> <main class="main-workspace" id="workspace"> <!--动态内容将在这里生成或拖入
--></main><aside class="right-panel"><form id="propertyForm" class="property-form">...</form></aside><footer class="status-bar"><span id="statusMessage">就绪</span><span class="hint">提示：尝试拖拽左侧项目，或使用Delete键删除</span></footer></div>

<script> // ===状态管理简易中心=== const state = selectedItemId: null, workspaceItems: [], // ...其他状态

; const listeners = []; function updateState(newState) Object.assign(state, newState); listeners.forEach(fn => fn(state)); //通知所有监听者

// === DOM引用与初始化=== document.addEventListener('DOMContentLoaded', () => const workspace = document.getElementById('workspace'); const resourceList = document.getElementById('resourceList'); // 1.初始化渲染模拟数据

// 2.绑定事件监听器

initDragAndDrop(workspace, resourceList); initKeyboardShortcuts(); // 3.启动模拟实时更新

startMockDataStream(); \);

// ===核心交互实现（例如：拖拽）=== function initDragAndDrop(sourceContainer, targetContainer) //详细实现拖拽逻辑，包括dragstart, dragover, drop事件处理

//更新state.workspaceItems，并调用updateState

// ===非线性路径示例=== function completeTaskViaPathA() /*路径A逻辑*/ function completeTaskViaPathB() /*路径B逻辑*/

// ===快捷键支持=== function initKeyboardShortcuts() document.addEventListener('keydown', (e) => if (e.key === 'Delete' && state.selectedItemId) //删除选中项的逻辑

\);

// ===模拟实时数据流=== function startMockDataStream() setInterval(() => //模拟服务器推送，随机更新一个项目状态

const mockUpdate = /*模拟数据*/ ; updateState( workspaceItems: newItems \); updateStatusMessage('数据已更新'); , 10000); //每10秒模拟一次

// ===工具函数=== function debounce(func, wait) /* ... */ function updateStatusMessage(msg) /* ... */ </script> </body> </html>
```