有这样一份流程

业务流程如下：

【$flow】

和

如下业务需求：

【$optimized_requirements】

请分析这个流程涉及到的数据模型，

要识别出来流程中数据模型的一对多的情况，

对于一对多的情况，要拆分出一个行程单的表，作为副表。

要充分考虑到主表的字段在副表中没有必要有重复的字段。

类似的，业务数据模型进行分析的时候要识别中可能出现的一对多情况，进行表的拆分。

申请单里则不要重复出现这些字段。

要充分考虑到申请人和使用人不一致的情况。

对应的各个单据模型，不管是主还是副单据模型，都要带有唯一的哈希码的单据号。

另外要充分考虑计划和实际之间的差别，要分别用字段进行记录

并且严格按照下列格式进行输出：

如果涉及到主模型还要关联其他模型的情况，请标记出来每个模型的主/副的标记

一个流程里只能有一个主的单据表，如果有其他的单据表里包含了这个主单据表的ID字段，则其他单据表为副单据表

分析副数据模型的时候要充分考虑其中涉及到的状态、类型等必要的属性。

并且为这些属性生成对应的候选选项

以及资源的分配如果是在主表进行分配，则对应的资源字段放在主表。而资源的分配如果在副表进行。则在副表生成对应的字段。

采用外键和副表相关联的方式。

也要分析出该数据模型是单据/资源/辅助信息/日志当中的哪种类型

以及有些数据的类型信息和对应的类型列表。

要充分挖掘字段，比如说：备注字段

充分发现对应的对称字段，比如说：起点、终点

要充分挖掘其他和业务需求相关联的其他字段

充分发掘执行的时候要填写的字段，比如：公里数等

还要避免生成和本需求，本业务无关的数据模型内容。

并且要分析出来其中的状态列表

一般的情况下，状态都是：未申请、已申请、已审批通过、已审批驳回、已开始执行、已结束执行、已取消、已强制取消、待重新分配等。

这些状态不要删除，遇到新的状态补充进来。

格式、范围、唯一等如果没有对的值的时候，输出&nbsp;

如果遇到多个步骤的情况，按照多流程进行扩展。

请严格按照下列格式进行输出

输出格式：

### 【模型】：模型名称（中文名称）（英文名称）

模型类型：主/副，只能从主/副二者选其一

模型类别：单据/资源/辅助信息

|字段名|英文|类型|主键|自增|必填|长度|格式|范围|唯一

|:---|:---|:---|:---|:---|:---|:---|:---|:---|:---###状态列表

### 类型列表